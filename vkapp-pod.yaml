apiVersion: v1
kind: Pod
metadata:
  name: vk-pod
  labels:
    app: vk-pod
spec:
  containers:
  - name: vk-busybox
    image: busybox
    command: ['sh', '-c', 'echo Hello Kubernetes! && sleep 3600']