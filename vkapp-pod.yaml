apiVersion: v1
kind: Pod
metadata:
  name: vkapp-pod
  labels:
    app: vkapp
spec:
  containers:
  - name: vkapp-busybox
    image: busybox
    command: ['sh', '-c', 'echo Hello Kubernetes! && sleep 3600']